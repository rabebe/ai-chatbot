# AI Chatbot

A conversational chatbot built with Python, LangChain, and OpenAI API.
This project will explore retrieval-augmented generation (RAG) and simple Flask API integration.

## Setup
1. Create a virtual environment: `python -m venv venv`
2. Activate it: `source venv/bin/activate`
3. Install dependencies: `pip install -r requirements.txt`